.DS_Store
.env
.env.*
node_modules/
.cache/
.netlify/
.vscode/
*.log

# Source assets (raw photos and large originals not shipped to web)
Image Gallery/
Pricing/
Reviews/
Logo.PNG
Logo Transparent Background.PNG
Desty - Portofolio .pdf
Deju-Studio-Developer-Guide.html
Deju-Studio-Developer-Guide.md
Deju-Online-Presence-Brief.md

# Exported point-of-sale materials (PDFs distributed to partners). Sources
# live in marketing/tabletent/<slug>.html; the PDF is a derived artefact.
POS Materials/

# Plan + memory (live outside repo)
.claude/

# Business-expansion workspace (strategy / investment planning, kept local-only)
business-expansion/

# Marketing flyer working files (kept locally, not committed). We DO commit the
# build pipeline (build/ templates + assets + build.sh + inline.py) and the final
# exported flyers, but not the raw photo sources, retired directions, or the
# large base64 build intermediates regenerated by inline.py.
marketing/flyer/_source/
marketing/flyer/_archive/
marketing/flyer/build/*.inlined.html
