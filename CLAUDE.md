# CLAUDE.md

Context for AI assistants picking up work on this repo. Read this once at the start of a session.

## What this is

Single-page marketing site for Deju Studio, an ultra-premium private nail studio in Ubud, Bali. Vanilla HTML/CSS/JS, no framework, no build step. Hosted on Netlify (auto-deploys from `main`). Bookings happen exclusively through WhatsApp. The backend is the careers application form (Netlify Forms, added 2026-06-23) plus one zero-dependency Netlify Function (`netlify/functions/submission-created.mjs`, an applicant auto-reply added 2026-06-24, currently dormant pending a DNS move) - see "Careers / hiring" below. There is no booking form, login, or other backend, and the function adds no build step.

## Current state

- **Live in production** at https://www.dejustudio.com with valid SSL (Let's Encrypt, Netlify-managed, auto-renews).
- **Analytics + conversion are live and verified.** GA4 stream `G-BZ8DVJJCNE` and Google Ads `AW-11529975683` (conversion label `tzL7CKzm76McEIPv9fkq`) are firing on every WhatsApp CTA click. Verified end-to-end on 2026-04-28 - GA4 `click_whatsapp` events landing, Ads conversion ping landing.
- **GA4 conversion tracking is confirmed healthy (verified 2026-06-23 via the MCP).** Over a rolling 90 days `click_whatsapp` fires ~170 times with ~130 counted as key events; since the campaign launched (2026-05-01) the **ad traffic alone produced ~49 WhatsApp conversions**. So the site + GA4 side works. **If the Google Ads dashboard shows almost no conversions, that's an Ads-side conversion-action misconfig, not a tracking bug** - check Ads -> Tools -> Conversions: the WhatsApp action's **Status** (active/verified) and whether it's **Primary** (the "Conversions" column only sums Primary actions), and that the campaign's goals include it. Don't let both the gtag action AND a GA4-imported `click_whatsapp` action count as Primary (double-counts). GA4 ↔ Ads are linked (customer `7732639350`). See `Summaries/2026-06-23-ga-conversion-diagnosis-and-cleanup.md`.
- **GA4 is queryable from Claude Code via the `analytics-mcp` MCP server** (set up 2026-06-23). Property ID `534840336`, authed with a service account (key at `~/deju-ga-mcp-key.json`, GCP project `deju-studio-analytics`) that has **Editor** on the property (upgraded from Viewer 2026-06-23). Use it to monitor traffic and the hiring campaign (sessions by source, `generate_lead` form submissions by `utm_source`). New MCP servers load at Claude Code startup, so a restart is needed after first registering it. **GA4 config writes** (key events, custom dims, etc.) are NOT done through the MCP (Data API / read only) but via the GA4 **Admin API** using the same key: mint a JWT with `openssl` (scope `analytics.edit`), exchange for a token, call `analyticsadmin.googleapis.com/v1beta/properties/534840336/...`. Full setup + the working Admin-API recipe in memory (`project_ga_mcp`).
- **Google Ads Search campaign is live and spending.** Campaign: "Deju Studio - Search - Ubud Tourists", 75,000 IDR/day, English, presence-based Ubud + 10km radius, Sat-Thu schedule. Maximize Clicks bidding with 12k IDR max CPC. Full plan at `/Users/conorsweeney/.claude/plans/ok-let-s-plan-this-toasty-cray.md`. Memory: `project_ads_campaign.md`.
- **Studio hours**: Sunday - Friday by appointment, Saturday closed (Saturday is Desty's day off). Bookable window in schema is 10:00-21:00. (Updated 2026-06-18 - was Mon-Sat / Sunday closed between 2026-05-14 and 2026-06-18; briefly Sat-Thu / Fri closed between 2026-04-28 and 2026-05-14.)
- **Mobile menu is fixed and verified.** The mobile nav overlay properly fixes to the viewport with a solid `--pure-bone` background, the logo on mobile is `transform: scale(2)` from a clamp(72px, 8vw, 96px) base, and link size is 24px display font. Verified on phone 2026-04-28. See "Architecture conventions" below for the backdrop-filter pitfall that caused earlier breakage.
- **Partner-attribution table tents are live (2026-05-04).** First partner is **Padma Warung** in Ubud. Mechanism: a `?ref=<slug>` URL param flips every WhatsApp CTA in that browser session to use the generic message template plus a `(Sent from <Partner>)` suffix. Slug-to-name map lives in `js/config.js` under `partnerNames`. The print artwork lives at `marketing/tabletent/padma-warung.html` (tri-fold prism, A6 panels, Instagram QR on the middle panel and booking QR on the back panel, both encoded with the brand's Forest-on-Bone-Cream palette). The exported print PDF lives at the main repo root in `POS Materials/` (gitignored).
- DNS records live in Wix (the domain registrar) but resolve to Netlify (A `75.2.60.5`, CNAME `www → dejustudio.netlify.app`). Wix does not own SSL or hosting; it is purely a DNS host.
- **Canonical domain is `www.dejustudio.com`** (flipped 2026-05-14, previously apex). The bare apex `dejustudio.com` 301-redirects to `www.`. The site's `<link rel="canonical">` and all schema URLs use `https://www.dejustudio.com/`. To change this, edit the Netlify site's `custom_domain` / `domain_aliases` via `netlify api updateSite`. **Don't mix canonical and apex URLs in schema/meta** - they need to match the primary or Google complains about canonical-via-redirect.
- **Google Search Console + Bing Webmaster Tools verified** (2026-05-14). Property type: URL prefix `https://www.dejustudio.com/`. Verification method: HTML meta tag (`<meta name="google-site-verification" content="of5QbFUejZV0uCoOjr3Odm4mjQTCdao2cA5JEiNWQIU">` in `<head>`). GA4 auto-verification does NOT work here because `js/analytics.js` deliberately loads gtag with the Ads ID in the script URL - GSC's static parser can't find a GA4 ID on the page. **If you ever re-verify GSC, use the meta tag method again**; don't waste time on GA4 auto-verify. Sitemap submitted at GSC and imported into Bing.
- Netlify CLI is installed locally and authenticated as `info.dejustudio@gmail.com` (Deju Studio team). Project ID: `8ff708dc-55db-4452-b5ba-16076b9d3320`. Use `netlify status` to confirm.
- Layout is single-page editorial (one `index.html`). **Section anchors** (in document order): `#top`, `#about` (Philosophy + Specialisations + Book CTA, merged 2026-05-14), `#artist` (About Desty - new 2026-05-14), `#services` (5-slide service carousel), `#gallery` (10-tile scattered grid with centre Book on WhatsApp CTA), `#visit` (contact info + map), `#reviews`, `#faq` (22-question accordion - new 2026-05-14), `#careers` (hiring section + application form - new 2026-06-23), `#book` (closing CTA). The earlier multi-page version was collapsed in commit `f9391fe`. Legacy `/about.html`, `/services.html`, `/gallery.html`, `/contact.html` are 301-redirected in `netlify.toml` to the new anchors - leave those redirects alone. `/careers` 301-redirects to `/#careers`, **plus tolerant aliases** (`/Careers`, `/career`, `/careers/*`, `/hiring`, `/jobs`, `/job`, `/apply`, `/apply-now`) added 2026-06-26 so no plausible hiring link can 404 - Netlify redirect matching is case-sensitive and exact, so each variant is listed explicitly. **Deep-anchor landing fix (2026-06-26):** opening the page at any `#anchor` (e.g. via the `/careers` redirect) used to park mobile visitors on the *previous* section, because lazy images + the map iframe above the target loaded late and pushed it down (careers was landing on `#faq`). `js/nav.js` now re-snaps to the hash target for ~3s after load (96px header offset, bails if the visitor scrolls), and the include is cache-busted as `nav.js?v=3`. See `Summaries/2026-06-26-careers-link-404-and-mobile-landing-fix.md`.
- **Careers / hiring (new 2026-06-23).** A `#careers` section in `index.html` carries the Nail Artist job copy plus a **Netlify Forms** application form (`name="careers-application"`, `data-netlify="true"`, multipart for an optional portfolio upload) that posts to `/careers-thanks` (a standalone success page that fires a GA4 `generate_lead` event). This is the site's only backend. A 5th JSON-LD block of `@type: JobPosting` sits in `<head>` (separate block per the JSON-LD convention) for Google Jobs. Form CSS is the `.careers-*` block at the end of `css/components.css`. **Live in production since 2026-06-23** (deployed via Netlify CLI from the Deju account, then merged to `main`). Applications **email** `info.dejustudio@gmail.com` (Gmail `Careers` label) via a Netlify form-submission hook (created with `netlify api createHookBySiteId`); the screening agent reads submissions straight from the **Netlify Forms API** (`form_id 6a39f024e9537d0008564fa1`), so a Google Sheet is optional, not required - and never the WhatsApp booking line. **Netlify form-detection gotcha:** the site had `processing_settings.ignore_html_forms = true` (off by default - it never had a form); it was flipped to `false` via `netlify api updateSite` on 2026-06-23 so the form registers. Form detection runs only on **production** deploys, not CLI draft previews. If submissions ever stop, check that setting first. Full wiring: `marketing/hiring-campaign/BACKEND-SETUP.md`. Meta ad plan + ad copy: `marketing/hiring-campaign/CAMPAIGN-BRIEF.md`. Hiring flyers: `marketing/flyer/build/hiring-editorial-*.html` (A4, Story, IG, FB - all four share one vertical-stack editorial layout with a written link CTA, **no QR**; design refreshed 2026-06-23, see `Summaries/2026-06-23-hiring-flyer-revisions.md`). The screening agent lives in the separate Agent Forge repo (`agents/app-screener/`). When the role closes, remove or update `validThrough` in the JobPosting block (Google flags expired postings). **Update 2026-06-24:** `generate_lead` is now a GA4 key event (verified firing); the Meta ad is built **Instagram-only** and pending review (brief switched IG-only, see `CAMPAIGN-BRIEF.md`); a 5th **1.91:1 landscape** hiring flyer was added; and an applicant **auto-reply** email function (`netlify/functions/submission-created.mjs`, via Resend) is committed but **dormant** - it needs Resend domain verification, which Wix DNS can't do (no subdomain MX) and Wix won't allow nameserver changes, so `dejustudio.com` is being **transferred off Wix** to reach Cloudflare DNS (auth code requested). See `Summaries/2026-06-24-ig-launch-keyevent-autoreply-domain-transfer.md` and `BACKEND-SETUP.md` Step 5.
- **SEO + GEO foundation is live** (shipped 2026-05-14). The page carries **4 JSON-LD blocks**: `BeautySalon` (with `@id`, geo coordinates, opening hours, areaServed, paymentAccepted, sameAs, founder cross-ref), `Person` (Desty Zulfa Nabila with bio, training, languages, portrait image), `OfferCatalog` (nested catalogs for Hands / Extensions / Feet / Add-on / Press-ons, 26 Offer entries mirroring visible prices), and `FAQPage` (22 questions). `/llms.txt` and `/llms-full.txt` (Markdown corpus for AI crawlers) sit at repo root. `robots.txt` explicitly allows 18 AI/search crawlers (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot, etc.). `sitemap.xml` includes a Google image sitemap. CSP and HSTS headers in `netlify.toml`. The full strategy brief lives at `Deju-Online-Presence-Brief.md` (gitignored).
- **Image shape language.** Hero / philosophy / artist / gallery images all use asymmetric `border-radius` blob shapes (each unique) with a subtle drop shadow. The `.gallery-scatter__item` tiles use per-`nth-of-type` rotation + translateY for a deliberate "scattered" feel that's stable across reloads.
- **Card payments carry a 3% surcharge** (disclosed 2026-06-18). The "What payment methods do you accept?" FAQ answer reads "Cash, card, bank transfer, and QRIS. Card payments carry a 3% surcharge." Same text in the visible `#faq` accordion AND the `FAQPage` JSON-LD (byte-identical, per the invariant), plus `llms.txt` / `llms-full.txt`.
- **Marketing flyers live in `marketing/flyer/` (committed 2026-06-18).** Two campaigns share one build pipeline (HTML templates -> `build/inline.py` data-URI inliner -> `build/build.sh` Chrome-headless export, same convention as the tabletents; rebuild all with `cd marketing/flyer && bash build/build.sh`). See `marketing/flyer/README.md` for the full system.
  - **Mani + Pedi / Refer-a-Friend promo (current).** Editorial direction only. One either/or offer (Mani + Pedi 10% off the pair **or** Refer a Friend, both 10% off next visit; open-ended). Four placements: Story 1080x1920, Instagram Ad 1080x1350, Facebook Ad 1080x1080, A4 WhatsApp (PDF + PNG). The offers sit **side by side** (two columns + an "or" divider). The **Bold** direction was tried and retired (kept in gitignored `_archive/`). Source photo: `_source/Mani Pedi Promo Pic.JPG`, resized into `build/assets/mani-pedi.jpg`. Write-up: `Summaries/2026-06-18-promo-flyers.md`.
  - **Press On Nails Workshop flyer (shipped 2026-06-13).** Editorial + Bold, each A4 + Instagram Story. Write-up: `Summaries/2026-06-13-workshop-flyer.md`.
  - **Two non-negotiable nail-art rules (global):** (1) every painted nail must stay fully visible - never cover nails with text/panels; keep the photo frame at the image's true aspect (the promo photo is 4:3). (2) No CSS `filter` over the nail art (no saturate/contrast/colour-grade); filters elsewhere are fine.
  - **Three print gotchas:** (1) Chrome print-to-PDF turns `box-shadow`/`text-shadow` into hard rectangles - use `filter: drop-shadow()` or solid-fill panels; (2) inverted (light-on-dark) QR codes scan in the PNG but FAIL in the PDF - use dark-on-light `qr-forest.png`; (3) always verify the actual PDF (macOS Quartz/Preview can differ from poppler: `qlmanage -t -s 2000 -o <dir> "<file>.pdf"`), not just the on-screen render.

## Architecture conventions

- **No build step.** Edit files, push to `main`, Netlify deploys. Do not introduce bundlers, package managers, or transpilers without checking with the user first.
- **`js/config.js` is the single source of truth** for the WhatsApp number, per-service message templates, and analytics IDs. CTAs throughout the site use `data-wa="<slug>"` attributes; `js/whatsapp.js` upgrades them with pre-filled `wa.me` links and per-service templated messages, and fires the conversion event on click.
- **Every `[data-wa]` anchor has a baseline `href="https://wa.me/6282340889808"` directly in HTML** as a no-JS fallback. `whatsapp.js` overwrites the href at load time with the templated message, but if it ever fails to load (extension blocking, network error), the CTA still opens WhatsApp. Don't strip these baseline hrefs.
- **`js/analytics.js` loads gtag using the Ads ID as the primary**, not the GA4 ID. This is a deliberate workaround: Google's gtag endpoint can return 404 for newly-created GA4 streams that haven't fully propagated server-side, which would block the entire gtag library from loading and break the Ads conversion as a side effect. See the comment in `analytics.js` and the commit message on `e1e7902`.
- **Gallery images** are managed via `scripts/optimize-images.sh`. The full curated library lives in `assets/img/` (JPG + WebP pairs, 1600w long edge). `index.html` only displays a subset; the rest are kept for future swaps. Don't delete unused images without asking.
- **Source assets** (raw photos in `Image Gallery/`, `Pricing/`, `Reviews/`, `POS Materials/`, original logo PNGs, the developer guide PDF, the SEO/GEO strategy brief at `Deju-Online-Presence-Brief.md`) are gitignored on purpose. They live locally only. Note: **session summaries under `Summaries/` are tracked** (committed to the repo as human-readable change history) - they are NOT gitignored.
- **FAQPage schema invariant.** The 22 FAQ answers exist in two places that MUST stay byte-identical: the visible `<details>/<summary>` accordion in the `#faq` section AND the `FAQPage` JSON-LD block in `<head>`. Google's rich-results eligibility requires the schema text to match the rendered DOM. When editing any FAQ answer, update both places in the same commit. There's a quick check: `python3 -c "..."` that asserts 0 mismatches - see git history for the verification snippet.
- **JSON-LD strategy: 4 separate blocks, not one `@graph`.** Each block (BeautySalon, Person, OfferCatalog, FAQPage) is independently lintable in Google's Rich Results Test, so one bad property won't poison the others. Cross-reference is done via `@id` URIs (e.g. BeautySalon's `founder` points to `#desty` which is the Person block's `@id`).
- **`llms.txt` / `llms-full.txt`** at repo root are the AI-crawler navigation + corpus files (Jeremy Howard's emerging standard). Keep them in sync with the visible site when service descriptions, pricing, hours, or FAQ answers change. The full corpus is `llms-full.txt`; the short index is `llms.txt`.
- **Deep-anchor landing + `nav.js` cache-busting.** Because the page is one long document with lazy images and a map iframe, landing on a deep `#anchor` (e.g. via the `/careers` redirect) is fragile: the browser jumps to the target at parse time, then late-loading media above it shifts the layout and parks the viewport on the wrong section. `js/nav.js` corrects this by re-snapping to `location.hash` for ~3s after load (50ms interval, 96px header offset, aborts on first user scroll). The `<script src="/js/nav.js?v=N">` include in `index.html` is **version-pinned** because `js/*` is served with `Cache-Control: max-age=86400` and the Instagram in-app browser caches aggressively. **When you edit `nav.js` (or any cached JS that must propagate immediately), bump the `?v=` number in `index.html`** so visitors fetch the new file instead of a stale copy.
- **Mobile menu / `backdrop-filter` pitfall.** `.site-header` only carries `backdrop-filter: blur(12px)` on desktop. On mobile it is explicitly disabled because `backdrop-filter` makes the element a containing block for `position: fixed` descendants (per CSS spec, same as `transform`/`filter`). With it enabled on mobile, the `.site-nav` overlay was being trapped inside the header's box, so the menu background didn't paint full-screen and the open menu overlapped the logo. If you re-enable `backdrop-filter` site-wide or move the nav into a different positioned ancestor, this bug comes back. The mobile logo also uses `transform: scale(2)` with `transform-origin: left center` so the layout box (and therefore the header height + the menu's `top: 96px` offset) stays unchanged.
- **Partner attribution flow.** Partners are added in two places only: `partnerNames` in `js/config.js` (slug -> display name), and a new `marketing/tabletent/<slug>.html` artwork file. `js/whatsapp.js` reads `?ref=<slug>` from the landing URL, persists it to `sessionStorage.dejuRef`, and on every `[data-wa]` CTA click overrides the per-service template with the generic one and appends ` (Sent from <Partner>)` so the studio sees a single recognisable opener in WhatsApp. GA4 also auto-captures the QR's `utm_source`/`utm_medium`/`utm_campaign` params, so traffic shows up in Acquisition reports. Non-partner visitors (no `?ref=`) keep the original per-service messages unchanged. Print artwork uses inlined `data:` URIs for the logo + QR codes so the HTML renders standalone (works under `file://` and detached from the repo). The exported PDF for distribution lives in the (gitignored) `POS Materials/` folder at the main repo root.

## Where to pick up next

Grouped by who owns the action. User-driven items happen outside the codebase; technical items are code changes.

### User-driven (outside this repo)

1. **Ongoing GBP maintenance.** Hours / address / services / Q&A / verification all done 2026-05-14. `aggregateRating` (5.0 / 149 reviews) added to `BeautySalon` schema same day. **Still ongoing:** review collection ritual (target 5+/month) and periodic photo refresh (every 2 months, 5-10 new shots). When `reviewCount` increments by ~25, bump the `aggregateRating.reviewCount` in `index.html` to match - it lives in the BeautySalon JSON-LD block in `<head>`.

2. **Update Google Ads ad-schedule** to Sun-Fri (matching current opening days; was Sat-Thu when campaign launched, and the studio's day off moved to Saturday on 2026-06-18). Don't do this during the campaign's bid-strategy learning phase - schedule edits reset learning. Wait until the campaign exits learning, then update.

3. **Submit "Deju Studio" for Google Ads business name verification** so ads display the trade name instead of a URL-derived placeholder. Path: ads.google.com -> Tools -> Billing or Setup -> Business name verification. Requires evidence (NIB / business registration, domain ownership, Instagram presence). Takes 3-21 days.

4. **Monitor the live ad campaign.** Campaign entered "Bid strategy learning" on 2026-04-28. Don't make material edits during the 7-10 day learning phase. Safe weekly tasks: prune Search Terms by adding negative keywords, review impression share. Once 15+ conversions accrue, switch bidding from Maximize Clicks to Maximize Conversions. Once 30+, switch to Target CPA. Full monitoring plan at `/Users/conorsweeney/.claude/plans/ok-let-s-plan-this-toasty-cray.md`.
   - **Open: fix the Ads "Conversions" column (2 min).** GA4 shows ~49 ad-driven WhatsApp conversions since 2026-05-01, but the Ads dashboard was reading ~1 (likely the 2026-04-28 verification click). Tracking is healthy (see the analytics state note up top) - the gap is an Ads-side conversion-action setting. In Ads -> Tools -> Conversions, check the WhatsApp action's **Status** and set it **Primary** (the column only sums Primary actions); ensure the campaign's goals include it; don't let both the gtag action and a GA4 import count (double-count). Note the bidding milestones above (15+/30+ conversions) depend on this column being right.

5. **Monitor Search Console.** GSC + Bing are verified and the sitemap is submitted. First crawl + indexing should be visible within days; first rich-results impressions (BeautySalon / FAQ / Person) typically appear 2-4 weeks post-submit. Check GSC -> Performance + Enhancements weekly.

6. **Local citations (NAP consistency)** on Apple Business Connect, TripAdvisor, Honeycombers Bali, Ubud Now & Then, The Bali Bible, NOW! Bali, Fresha or Booksy, Foursquare. Identical name / address / phone everywhere. Full list in `Deju-Online-Presence-Brief.md`.

7. **Track Padma Warung commission.** Tabletents printed and delivered 2026-05-14. Every WhatsApp inquiry that arrived from the table tent will end with `(Sent from Padma Warung)`. Reconcile those against bookings completed and pay commission per the partner agreement. GA4 also reports the same traffic under `source = padma-warung` for sanity-checking volume. If you ever need to re-print or edit the artwork: source HTML is at `marketing/tabletent/padma-warung.html`, exported PDF at `POS Materials/Deju Studio - Tabletent for Padma Warung.pdf` (gitignored). Specs to the Bali print shop: 300-350 gsm uncoated matte card, single-sided, trim to 325x148 mm with 3 mm bleed, fold at the three vertical fold lines, glue the 10 mm tab inside the back panel.

8. **Launch + monitor the Nail Artist hiring campaign.** Careers funnel is live and the GA4 conversion is verified. What remains, in priority order:
   - **Careers links + landing - DONE 2026-06-26.** Reported as "site down" from Instagram; it was a path-specific 404 (case-sensitive redirect), not an outage. Fixed by adding tolerant `/careers` aliases in `netlify.toml` and a deep-anchor re-snap in `js/nav.js` (see "Section anchors" + "Deep-anchor landing" above). **Canonical links:** paid IG ad = `https://www.dejustudio.com/careers?utm_source=instagram&utm_medium=paid&utm_campaign=hiring` (already set, correct); **Desty's organic posts** (IG bio, Story stickers, captions) = `https://www.dejustudio.com/careers?utm_source=ig&utm_medium=social&utm_campaign=hiring` (`utm_source=ig` keeps organic distinct from paid in GA4). Note: GA4 can't tell whether the ad points at `/careers` vs `/` (the redirect resolves to `/` and GA strips utm + the `#careers` fragment), so don't read the GA landing-page dimension as evidence the ad is misrouted - it isn't.
   - **Applicant auto-reply email - finish the domain move (the main open task).** `netlify/functions/submission-created.mjs` (Resend) is committed but **dormant**. Blocked because Resend's domain verification needs a subdomain MX that **Wix DNS can't add**, and **Wix won't allow nameserver changes** - so `dejustudio.com` is being **transferred off Wix** to reach Cloudflare DNS. Auth code requested from Wix 2026-06-24. **Resume:** (1) get the auth code + confirm the domain is unlocked at Wix; (2) transfer to a transfer-friendly registrar (Porkbun/Namecheap - note a *direct* transfer to Cloudflare Registrar is impossible until the domain is already on Cloudflare nameservers); (3) at the new registrar, set nameservers to Cloudflare's `blakely.ns.cloudflare.com` / `harvey.ns.cloudflare.com` (the Cloudflare zone is already built with the two DNS-only records); (4) add Resend's DNS records in Cloudflare and verify; (5) set `RESEND_API_KEY` as a Netlify env var; (6) push to deploy, submit a real application to test, delete the test submission. Full step-by-step: `BACKEND-SETUP.md` Step 5 + `Summaries/2026-06-24-...md`. **Lighter fallback if the transfer stalls:** switch the function from Resend to SendGrid or Brevo, which verify via CNAME/TXT records Wix *does* support - then no transfer is needed (we chose the transfer for the longer-term benefit of leaving Wix).
   - **Meta Instagram ad - built, pending Meta review.** Set up 2026-06-24 as a **manual, Instagram-only** Traffic campaign (landing-page-views goal, Ubud + Gianyar, IG-only placements, A/B = Story + 1080x1350, ~75k IDR/day x ~13 days, ends ~6 July 2026, destination `utm_source=instagram`). Full plan: `CAMPAIGN-BRIEF.md`. Once approved: watch CPC / landing-page-views for the first 3 days, pause the weaker creative, and do the **organic amplification** (Story to IG Stories, pin the 1080x1350 to the feed, careers link in the IG bio, A4 as Desty's WhatsApp Status). Note the live ad campaign's end date 6 July 2026 and the JobPosting `validThrough` to refresh when the role closes.
   - **GA4 `generate_lead`: DONE + verified 2026-06-24** (key event created via the Admin API, `ONCE_PER_SESSION`; a real test submission produced 1 event / 1 key event). Monitor via the `analytics-mcp` (sessions + `generate_lead` split by `utm_source`: `instagram` = paid IG, `ig`/`social` = organic IG).
   - **Screen applications** with the `app-screener` agent in the separate Agent Forge repo (`~/Agent Forge/agents/app-screener/`) once they arrive: it reads the Netlify submissions, scores against the role rubric, and ranks. Validate its calls against your own judgment on the first ~10 before trusting it (Phase 2 readiness gate).
   - Optional: the Google Sheet mirror (only if you want a spreadsheet view - `marketing/hiring-campaign/BACKEND-SETUP.md`). The Gmail `Careers` label/filter is already set up.

### Technical (code changes - smaller scope now)

9. **`js/config.js` has dead fields.** `address`, `hours`, `mapEmbedSrc`, `mapsUrl`, `whatsappDisplay`, `instagram`, `instagramUrl`, `email`, `googleBusinessUrl` are defined but no JS reads them - `index.html` hardcodes the values directly. Only `whatsappNumber`, `whatsappTemplates`, `analytics`, and the new `partnerNames` are actually read (by `whatsapp.js` and `analytics.js`). Two valid futures: wire them up so config is canonical, or delete. **Ask before removing.**

10. **Adding more partner table tents.** When a new partner signs up: (a) add `<slug>: '<Display Name>'` to `partnerNames` in `js/config.js`, (b) generate a new QR with `qrencode -o assets/img/qr-<slug>.svg -t SVG -l Q -m 2 --foreground=1E5128 --background=F2EBDD "https://www.dejustudio.com/?ref=<slug>&utm_source=<slug>&utm_medium=qr&utm_campaign=tabletent"`, (c) duplicate `marketing/tabletent/padma-warung.html`, find/replace the slug + name + inline data URIs (use the existing python helper pattern in `marketing/tabletent/README.md`), (d) export to PDF and drop in `POS Materials/`. The tabletent README has the full step-by-step.

11. **Favicon + manifest + apple-touch-icon set.** Currently only `assets/logo/logo-forest.png` is wired as the `<link rel="icon">`. The plan calls for a full icon set generated via realfavicongenerator.net (favicon.ico, favicon-16.png, favicon-32.png, apple-touch-icon-180.png, icon-192.png, icon-512.png, maskable icon, manifest.json). Easy 30-minute follow-up once the icon set is generated.

12. **Stale CSS cleanup (mostly done 2026-06-23).** The biggest dead blocks from the multi-page collapse were removed after verifying each class had 0 references in `index.html`, `careers-thanks.html`, and `js/` (`.service-card*`, `.service-grid`, `.pillars`/`.pillar`, `.filter-bar*`, `.menu-item__book`, `.menu-section` base, `.about-hero*`, `.founder-letter`, `.home-pillars`, `.home-services` - ~132 lines). If you do another pass, use the same method (grep the class against all loaded HTML/JS; a selector that matches nothing in the DOM is safe to drop since there's no build step) and a couple of legacy names may still linger (`.divider-brass`, `.stack`).

13. **(Optional) Replace `assets/logo/logo-cream.png` with an official Bone Cream variant** when one is available.

14. **Flyer follow-ups (`marketing/flyer/`).** The `marketing/` tree is now committed (build pipeline + final flyers; raw photos/inspiration/drafts live in gitignored `_source/`, retired directions in gitignored `_archive/`). Open items:
    - **Promo (current):** print-test the A4 at the Bali shop and scan the photo for colour before any run; otherwise the four placements are final. If a partner/seasonal variant is needed, copy the closest `build/promo-editorial-*.html`. Write-up: `Summaries/2026-06-18-promo-flyers.md`.
    - **Workshop (Bold A4 QR fix - still open):** `build/photographic-a4.html` uses the inverted `qr-cream.png`, which scans in the PNG but fails in the PDF print path; swap its `<img>` to `qr-forest.png` and rebuild (the other 3 workshop deliverables already use it). Conor was asked and hadn't decided. Then pick a final workshop direction (Editorial vs Bold) and print-test.
    - **Hiring (design final 2026-06-23):** all four placements (A4, Story, IG, FB) share one vertical-stack editorial layout - centered logo → centered nail photo (`build/assets/hiring-1-centered.jpg`, a square centered crop of `hiring-1.jpg`) → "Nail Artist" headline → "What you bring / What we offer" columns → centered green footer (link CTA, **no QR**) with a cream gap. The Story reserves a clear band + an "Apply at the link" cue for an Instagram link sticker (added manually before posting). Print-test the A4 before any run; otherwise final. To re-center or re-crop the photo, regenerate `hiring-1-centered.jpg` from `hiring-1.jpg` (or the raw `_source/Hiring/`). Write-up: `Summaries/2026-06-23-hiring-flyer-revisions.md`.
    - To rebuild any flyer: `cd marketing/flyer && bash build/build.sh`, and **always check the exported PDF** (`qlmanage -t -s 2000 -o <dir> "<file>.pdf"`), not just the on-screen render - the shadow/QR print gotchas only show up in the PDF.

15. **(Deferred) Stand up a Google Ads MCP** for programmatic spend/conversion monitoring from Claude Code, alongside the GA4 `analytics-mcp`. Wanted so the Ads "Conversions" mismatch (item 4) and campaign metrics can be inspected here instead of only in the Ads UI. **Not a quick task** - the Google Ads API needs an approved **developer token** (apply inside an Ads Manager/MCC account; days of lead time) plus an **OAuth client + refresh token** for `info.dejustudio@gmail.com` (the GA service account can't auth Ads - Ads on a personal Gmail doesn't accept service-account auth). Treat as its own ~half-day setup session. Raised + deferred 2026-06-23.

## How to resume work

### Orient yourself

1. `cd "/Users/conorsweeney/Deju Studio OS"`
2. `git status` and `git log --oneline -10` to see what's changed recently.
3. `netlify status` to confirm CLI auth and which site is linked.

### Edit and preview

4. For local preview: `python3 -m http.server 5173` and open `http://localhost:5173`. No build step - edit files, refresh browser.
5. To test on a real phone, your laptop's LAN IP works: `python3 -m http.server 5173 --bind 0.0.0.0` then open `http://<your-laptop-ip>:5173` from the phone (same Wi-Fi).
6. Most copy and structure lives in `index.html`. Site-wide config (WhatsApp number, message templates, analytics IDs) lives in `js/config.js`. Visual tokens (colours, type, spacing) live in `css/tokens.css`.

### Deploy

7. Push to `main`. Netlify auto-deploys in ~1-2 min.
8. Verify a deploy completed: `netlify api listSiteDeploys --data '{"site_id":"8ff708dc-55db-4452-b5ba-16076b9d3320"}'` and check the top entry has `state: "ready"` matching your latest commit SHA.
9. After deploy, hard-reload to bypass cache. On desktop: Cmd+Shift+R. On iOS Safari: easiest is to open the site in a Private tab (long-press tabs icon -> New Private Tab). On iOS Chrome: open in an Incognito tab. Private/Incognito tabs always fetch fresh CSS/JS.

### Test the analytics + conversion path

10. Open the site in a clean browser (no ad blockers / privacy extensions), open DevTools Network tab, filter `google`, click any WhatsApp CTA. Expect:
   - `gtag/js?id=AW-...` (200)
   - `collect?v=2&tid=G-BZ8DVJJCNE&...` (204)
   - `googleads.g.doubleclick.net/pagead/...` request firing
   - If `gtag/js` fails with `ERR_BLOCKED_BY_CLIENT`, that's the testing browser's extension or system-level network blocker, not a site bug.

### Check careers applications + GA (hiring campaign)

- **Read applications:** `netlify api listFormSubmissions --data '{"form_id":"6a39f024e9537d0008564fa1"}'` (each submission's `data` object holds the fields). They also email `info.dejustudio@gmail.com` under the `Careers` label.
- **Query GA4:** the `analytics-mcp` server exposes the Deju property (`534840336`). Restart Claude Code first if it was just registered, then ask for sessions by source or `generate_lead` counts by `utm_source` to see how the hiring campaign is performing. If the MCP fails to connect, check `~/deju-ga-mcp-key.json` exists and `claude mcp list` shows it.
- **Test the form locally:** `python3 -m http.server 5173`, open `/#careers`, submit. Note: Netlify Forms only *captures* on production deploys, so a real submission test must be done on `www.dejustudio.com` (it lands in the Netlify Forms dashboard + emails).

### Test the partner attribution path

11. Open `http://localhost:5173/?ref=padma-warung`, click any WhatsApp CTA. The pre-filled message should be the generic template plus ` (Sent from Padma Warung)`.
12. With the partner-tagged session active, click an in-page anchor (e.g. Services). The URL strips back, but the next WhatsApp click should still carry the tag (sessionStorage persistence).
13. Open `http://localhost:5173/` with no params. WhatsApp messages should be the per-service templates with **no** trailing tag (regression check).
14. To test the print artwork without serving from the repo, open `marketing/tabletent/padma-warung.html` directly in Chrome via `file://`. The logo + both QR codes are inlined as data URIs so the page must render fully without any local server.

### Re-export the print PDF after editing the artwork

15. Open the partner's HTML file in Chrome (Safari is unreliable for custom mm page sizes). `Cmd+P` -> Save as PDF, page size **Custom 331 x 154 mm**, margins **None**, background graphics **On**. Save into `POS Materials/` overwriting the existing PDF. Verify in Preview.app -> Tools -> Show Inspector -> page size reads exactly 331 x 154 mm.

### Rebuild the marketing flyers

16. `cd marketing/flyer && bash build/build.sh` rebuilds every flyer (promo + workshop): inlines each `build/*.html` template's assets, then exports the PNG/PDF deliverables into `marketing/flyer/`. Templates and assets live in `build/`. **Always verify the exported PDF, not just the PNG** (Quartz/Preview can differ from poppler). Full system + the two nail-art rules: `marketing/flyer/README.md`.

## Things to avoid

- Don't add em dashes anywhere. Use a hyphen instead. (Global rule from user memory.)
- Don't introduce a build step or `package.json`. **Specifically: don't `npm i netlify-cli` inside this directory** - it creates `node_modules/` and `package.json`. Install globally (`brew install netlify-cli`) instead.
- Don't delete unused gallery images in `assets/img/` without asking - they are the curated library.
- Don't strip the baseline `href="https://wa.me/..."` from `[data-wa]` anchors in `index.html` - they are deliberate no-JS fallbacks.
- Don't change `js/analytics.js` to use the GA4 ID as the gtag/js primary - this would re-break the Ads conversion. See the comment in that file.
- Don't re-enable `backdrop-filter` on `.site-header` at mobile widths, and don't move `.site-nav` into a different positioned ancestor without checking what containing block its `position: fixed` ends up in. This re-breaks the mobile menu (background fails to paint full-screen, menu overlaps the logo). See "Architecture conventions" above.
- Don't touch the `[[redirects]]` block in `netlify.toml` that maps legacy multi-page URLs to in-page anchors. This now also includes the tolerant `/careers` aliases (`/Careers`, `/career`, `/careers/*`, `/hiring`, `/jobs`, `/job`, `/apply`, `/apply-now`) - **don't remove them**; they stop hiring links from 404ing, and Netlify matching is case-sensitive so each must stay listed.
- **Don't edit `js/nav.js` (or other cached JS) without bumping the `?v=` query in its `index.html` include.** `js/*` is served with a 1-day cache and the Instagram in-app browser caches harder; without the bump, visitors keep running the old script. Also don't remove the deep-anchor re-snap in `nav.js` - it's what keeps `/careers` (and any `#anchor`) from parking mobile visitors on the wrong section.
- Don't commit anything from the gitignored list (`Image Gallery/`, `Pricing/`, `Reviews/`, `POS Materials/`, `Logo*.PNG`, `Deju-Studio-Developer-Guide.*`, `Desty - Portofolio .pdf`, `Deju-Online-Presence-Brief.md`).
- Don't break the partner-attribution flow when editing `js/whatsapp.js`. Specifically: when a partner ref is in sessionStorage, the message body must use the **generic** template (not the per-service one) plus ` (Sent from <Partner>)` on the same line. Service-specific templates only fire for non-partner traffic. Tests in `CLAUDE.md` -> "Test the partner attribution path".
- Don't strip the inline `data:image/png;base64,...` and `data:image/svg+xml;base64,...` URIs from `marketing/tabletent/*.html` files. They're inlined deliberately so the artwork renders standalone (under `file://`, in print preview, and outside the repo).
- Don't make material changes (keywords, bids, budget +/- 20%, schedule, locations, bid strategy) to the live Google Ads campaign during the learning phase - it resets learning and sets the campaign back another week.
- **Don't edit FAQ answers in only one place.** Visible HTML in the `#faq` section AND the matching `FAQPage` JSON-LD answer text must be byte-identical for Google's FAQ rich-results eligibility. Always update both in the same commit.
- **Don't invent `aggregateRating` numbers** for the BeautySalon schema. Add it only with verifiable counts from the live GBP page.
- **Don't remove the `<meta name="google-site-verification" content="of5Q...">` tag** from `<head>` without first re-verifying Search Console via a different method. Removing it un-verifies the property, which suspends Search Console reports and pauses sitemap recrawl. If you re-verify, the easiest path is to keep this tag in place and just leave it.
- **Don't change the canonical domain back to apex** without also updating every `https://www.dejustudio.com/...` URL in the schema, OG tags, sitemap, and `llms*.txt` to match. The current canonical (`www.dejustudio.com`) is set on Netlify via `custom_domain`; the apex is now an alias that 301-redirects to www.
- **Don't cover the painted nails with text/overlays in any flyer, and don't put any CSS `filter` over the nail art** (no saturate/contrast/colour-grade). Keep the photo frame at the source image's true aspect ratio so every nail shows. See `marketing/flyer/README.md`.
- **Don't commit `marketing/flyer/_source/` or `_archive/`** (raw photos, inspiration refs, retired directions) or the `marketing/flyer/build/*.inlined.html` build intermediates - they are gitignored on purpose.
